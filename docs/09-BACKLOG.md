# 09 — Backlog

Tarefas na ordem de execução. Cada bloco é uma sessão de Claude Code. Marque conforme concluir.

## Fase 1 — Base (D1)

- [x] **T01** `create-next-app` com TypeScript, Tailwind, App Router, ESLint. Configurar `tsconfig` estrito.
- [x] **T02** Tokens do doc 07 no `tailwind.config.ts` (cores nomeadas, escala de tipo) e fontes via `next/font`. _(Next 16 veio com Tailwind v4 — tokens em `@theme` no `globals.css`, não em `tailwind.config.ts`; mesmo resultado.)_
- [x] **T03** Projeto Supabase + rodar o SQL do doc 03 + ligar RLS. Guardar as chaves.
- [x] **T04** `lib/supabase/server.ts` com service role. Adicionar comentário no topo: uso exclusivo em servidor.
- [x] **T05** `content/evento.ts`, `content/casal.ts`, `content/faq.ts` com placeholders tipados.
- [x] **T06** Layout raiz: cabeçalho, rodapé com link de privacidade, barra fixa de ações no mobile, metadados e Open Graph (a prévia no WhatsApp importa mais que qualquer outra coisa aqui).
- [x] **T06b** Navegação: barra superior (monograma + menu, transparente sobre o hero e sólida ao rolar), painel de menu em tela cheia, versão desktop com destinos e botão de confirmar presença. Alvo visual: `mockups/08-menu.png` e `mockups/09-desktop.png`.
- [x] **T07** Home: hero com contagem regressiva, história, cerimônia e festa com link de mapa, FAQ em acordeão.
- [x] **T08** Deploy inicial na Vercel com variáveis de ambiente.

## Fase 2 — Formulários (D2)

- [x] **T09** `lib/validations.ts` com os schemas Zod de RSVP, recado e checkout.
- [x] **T10** `lib/rate-limit.ts` por IP, em memória com janela deslizante (suficiente nesta escala).
- [x] **T11** Turnstile: componente no cliente + verificação do token no servidor. _(Some da tela até `NEXT_PUBLIC_TURNSTILE_SITE_KEY` ser cadastrada — conta ainda não criada.)_
- [x] **T12** RSVP: página, formulário, Server Action com honeypot, upsert por telefone normalizado, tela de sucesso, checkbox de ciência de privacidade.
- [x] **T13** Mural: listar só `approved = true and hidden = false`, formulário de envio criando registro pendente, mensagem de estado vazio.
- [x] **T14** ~~`middleware.ts`~~ `proxy.ts` (Next 16 renomeou) protegendo `/painel`; página de login com senha, cookie assinado, rate limit e comparação em tempo constante.
- [x] **T15** Painel — aba Confirmações: tabela, totais (confirmados, recusados, total de pessoas), busca, exportar CSV.
- [x] **T16** Painel — aba Recados: aprovar, ocultar, excluir.

## Fase 3 — Presentes e checkout (D3)

- [x] **T17** Seed do catálogo (doc 06) e `lib/money.ts` (centavos ↔ BRL). _(Precisou de uma coluna nova, `gifts.category`, para o filtro por bloco da T18 — doc 03 atualizado.)_
- [x] **T18** Página da lista: filtro por bloco, cartão com barra de progresso nas cotas, presente único esgotado aparece marcado, não some.
- [x] **T19** Página de detalhe: formulário de dados do presenteador (nome, e-mail opcional, recado, mostrar meu nome), seletor de quantidade quando for cota.
- [x] **T20** `POST /api/checkout` implementando os 10 passos do doc 04, na ordem. Testado: preço adulterado no corpo é ignorado (usa sempre `gift.price_cents` do banco). **Falta testar a criação real da preference** — sem `MP_ACCESS_TOKEN`, o endpoint responde 503 de forma controlada.
- [x] **T21** Redirect para `init_point` com estado de carregamento e tratamento de erro. _(Já estava implementado em `PresentearForm.tsx` desde a T19/T20 — checkbox só estava desatualizado. Confirmado no navegador: erro 503 controlado exibe "Pagamento indisponível no momento" e o botão volta ao normal.)_
- [x] **T22** `/presentes/obrigado`: consulta de status a cada 3 s por até 60 s, três estados (confirmando, confirmado, algo deu errado). _(Nova rota `GET /api/orders/[id]` só leitura, nunca escreve status. Testado no navegador: criei uma order de teste via `/api/checkout`, vi o estado "confirmando", marquei `status='paid'` direto no Supabase e confirmei a virada automática para "confirmado" no próximo poll; testei também o estado de erro sem `order` na URL. Order de teste removida depois.)_

## Fase 4 — Webhook e e-mails (D4)

- [x] **T23** `POST /api/webhooks/mercadopago`: validar `x-signature` (com `data.id` em minúsculas), rejeitar timestamp velho, responder 401 se inválido. _(Usa `WebhookSignatureValidator` oficial do pacote `mercadopago` em vez de HMAC na mão — mesma lógica do doc 04, com comparação em tempo constante. Testado com `curl` contra o app de teste: assinatura válida → 200, forjada → 401, ausente → 401, timestamp de 20 min atrás → 401. Processamento do pagamento fica para T24/T25.)_
- [x] **T24** Idempotência: gravar `data.id` em `webhook_events`; duplicata responde 200 e sai. _(Insert com índice único; violação `23505` = duplicata, trata como sucesso sem reprocessar.)_
- [x] **T25** Consultar o pagamento na API, conferir valor contra `total_cents`, aplicar transição de status em transação, incrementar `quota_sold` quando aprovado. _(`approved` com valor batendo → `paid` + incrementa `quota_sold`; `rejected` → `failed`; valor divergente não marca como pago, mas grava em `payments` para conciliação. Testado de ponta a ponta em sandbox: criei order real, tokenizei cartão de teste aprovado (`APRO`), paguei via API do MP, disparei o webhook de verdade contra o app rodando local — order virou `paid`, `quota_sold` incrementou; reenvio da mesma notificação não duplicou o incremento; um pagamento aprovado com valor diferente do `total_cents` da order ficou registrado em `payments` mas a order continuou `pending`. Refund/estorno não é tratado aqui — cai na rotina de conciliação do doc 04.)_
- [x] **T26** E-mails via Resend: recibo ao convidado (se informou e-mail), aviso de presente ao casal, aviso de confirmação de presença e de recado novo — todos imediatos. _(`lib/resend.ts` + `lib/emails.ts`, templates HTML inline no estilo do doc 07. Todos disparados na hora — no webhook (presente), em `presenca/actions.ts` (RSVP) e em `recados/actions.ts` (mural) — nunca bloqueiam a resposta ao usuário, falha vira log genérico sem dado de convidado. **Histórico:** a versão original tinha um digest diário via cron (`GET /api/cron/digest-confirmacoes`) em vez de aviso imediato de RSVP; removido em 10/08 a pedido do casal, que preferiu saber na hora. **Pendência real, não bug**: sem domínio verificado no Resend, o remetente `onboarding@resend.dev` só entrega para o e-mail da própria conta — o recibo ao convidado só funciona de verdade depois que o domínio do site for verificado no Resend (mesmo prazo do domínio, ~13/08). Os avisos ao casal já funcionam porque `CASAL_EMAIL` é o e-mail da conta. Testado de ponta a ponta com pagamento real, RSVP real e recado real.)_
- [x] **T27** Painel — aba Presentes: pagos, pendentes, total arrecadado, quem presenteou o quê, recados dos presentes. _(`app/painel/(dashboard)/presentes/`: tiles de pagos/pendentes/total arrecadado, busca por comprador ou presente, tabela com presente, quem presenteou (indicando "anônimo no site" quando `show_name=false`), quantidade, valor, status, método de pagamento e recado. Testado no navegador logado no painel, com dado real e com o estado vazio.)_
- [x] **T28** Comprovante na tela de agradecimento, com botão de salvar imagem. _(`Comprovante.tsx` em `presentes/obrigado/`: cartão de presente dentro da `<Moldura>`, nomes do casal em `font-caligrafica`, monograma M&J, data do evento — bate com `mockups/05-confirmacao.png`. `GET /api/orders/[id]` passou a devolver os dados do presente/comprador só quando `status=paid`. Botão "Salvar imagem" usa a lib `html-to-image` (nova dependência, leve, sem sub-dependências) para gerar um PNG do cartão via canvas. Testado no navegador de ponta a ponta: criei uma order paga de verdade, a tela renderizou o cartão certo, cliquei em "Salvar imagem" e o PNG baixado bate pixel a pixel com o layout esperado — sem a margem em branco que apareceu na primeira tentativa, já corrigida.)_
- [x] **T29** Testes 1 a 4 do doc 04 em sandbox, com túnel público para o webhook. _(Instalei `cloudflared` (túnel rápido, sem precisar de conta) e apontei `NEXT_PUBLIC_SITE_URL` + o webhook sandbox do MP pra ele. **Teste 1, o que realmente importava**: criei um pedido de verdade, paguei via API com o cartão de teste oficial do MP (`5031 7557...`, titular `APRO`) e **não cutuquei o webhook manualmente** — o Mercado Pago mandou a notificação sozinho pro túnel em ~2s, com assinatura genuína (não fabricada por mim), e o pedido virou `paid`, a cota incrementou e os e-mails saíram, tudo automático. Isso descarta a dúvida que o próprio doc do MP levanta ("pagamentos de teste não enviam notificação") — pelo menos com o app configurado via *Suas integrações*, envia sim. Testes 2 (duplicata), 3 (assinatura falsa) e 4 (preço adulterado) já tinham sido validados nas T23-T25 e reconfirmei o de duplicata com o payment id real. Também descobri que o Checkout Pro hospedado (Pix e "Pagar" com cartão salvo) não respondia a clique automatizado do browser — parece proteção anti-bot da própria página do MP, não bug nosso; contornei validando a criação do pagamento direto pela API, que é o que efetivamente teria acontecido depois do convidado preencher os dados no checkout. Ambiente revertido ao final: `NEXT_PUBLIC_SITE_URL` de volta pra localhost, webhook sandbox do MP de volta pra URL da Vercel, túnel e dev server encerrados, banco limpo.)_

## Fase 5 — Produção (D5)

- [x] **T30** Substituir todos os placeholders pelo conteúdo real; otimizar as fotos. _(Parcial 10/08: removidas as 3 imagens placeholder órfãs que sobraram de antes das fotos reais chegarem (nenhum código mais referenciava); fotos reais já servidas via `next/image` (otimização automática de tamanho/formato já ativa, nada a fazer aí); limpo texto TODO morto em `Local.tsx` (o horário de chegada já está confirmado, o branch TODO nunca rodava, só não tinha sido removido); comentário desatualizado em `api/checkout/route.ts` corrigido. Catálogo de presentes com valores aprovados pelo casal em 10/08 — os 21 preços atualizados direto no banco de produção via script pontual (títulos e textos das piadas mantidos, doc 06 atualizado para refletir). **Fechado em 10/08**: Bruno decidiu manter a foto do hero como imagem do Open Graph em vez da arte do convite — não vamos trocar, o TODO em `app/layout.tsx` pode ficar ou ser removido, mas não é mais pendência.)_
- [x] **T31** Cabeçalhos de segurança e CSP no `next.config`. _(CSP restritiva liberando só `challenges.cloudflare.com` (Turnstile) em script/connect/frame-src; `style-src` com `unsafe-inline` (progress bar usa `style` inline); HSTS, `X-Content-Type-Options`, `Referrer-Policy`, `X-Frame-Options: DENY`. Testado com `curl -I` contra build de produção real (`next start`) confirmando os 5 headers, e navegação completa pelo site (home, presença, presentes, presente individual, recados, privacidade, painel) sem nenhuma violação de CSP no console.)_
- [x] **T32** Página `/privacidade` e link no rodapé e nos formulários. _(`app/(site)/privacidade/page.tsx`, conteúdo direto do doc 05: o que coleta, para quê, com quem compartilha (MP/Vercel/Supabase), por quanto tempo guarda, como pedir exclusão. Link já existia no rodapé e no RSVP (T12); adicionei no formulário de recado e no de presentear, que não tinham.)_
- [x] **T33** Domínio apontado, credenciais de produção, webhook de produção cadastrado no painel do Mercado Pago. _(Feito em 10/08. Aplicação **Mayejhow** (nº 7683948047482322) do casal, Bruno com acesso de colaborador. `MP_ACCESS_TOKEN` e `MP_WEBHOOK_SECRET` de produção configurados na Vercel (Production). **Domínio final decidido em 10/08: sem domínio próprio** — projeto Vercel renomeado de `site-casamento` para `jhowemayara`, domínio `https://jhowemayara.vercel.app` adicionado ao projeto e servindo produção. `NEXT_PUBLIC_SITE_URL` atualizado e redeploy de produção feito (OG e imagens já refletem o novo domínio). Bruno recadastrou a URL de notificação no painel do MP para `https://jhowemayara.vercel.app/api/webhooks/mercadopago`; endpoint testado com `curl` no novo domínio, respondendo 401 a assinatura inválida como esperado. URL antiga (`site-casamento-lyart.vercel.app`) segue no ar em paralelo, sem downtime.)_
- [x] **T34** Checklist de segurança do doc 05, item por item. _(Os 9 itens testados/confirmados em 10/08 e marcados no próprio doc 05: grep de segredo no client + no bundle de produção, preço adulterado ignorado (testado com gift real), assinatura de webhook inválida → 401, duplicata não reprocessa, recado não aprovado não aparece (por query), rate limit do RSVP testado isoladamente (100 chamadas → 5 permitidas, 95 bloqueadas, batendo com a config real), `/painel` sem cookie → 307 para login, privacidade publicada, cabeçalhos em produção.)_
- [x] **T35** Teste real com dinheiro (Pix R$ 5,00 e cartão), conferindo o crédito na conta do casal. _(Feito pelo Bruno em 10/08, R$5 em cada método real, presente "Contribuição livre" (ajustado pra R$5 pra esse teste). Pix confirmou e caiu na conta do casal, mas revelou um bug real: o pedido ficava `pending` mesmo com o dinheiro creditado — a idempotência do webhook (T24) descartava a notificação de aprovação como "duplicata" da de criação, porque as duas compartilham o mesmo `data.id`. Corrigido na hora (chave agora inclui o status), reconciliado o pedido preso, e o cartão confirmou normalmente. Ver commit "Corrige idempotência do webhook".)_
- [x] **T36** Revisão no celular junto com o casal. Ajustes finais. Publicar. _(Aceite final do Bruno e do casal em 10/08, sem ajustes pedidos. Site em produção: `https://jhowemayara.vercel.app`.)_

## Prompt inicial sugerido para o Claude Code

```
Leia CLAUDE.md e docs/01 a 09 antes de começar.
Execute a tarefa T01 do docs/09-BACKLOG.md.
Não avance para a próxima tarefa sem eu confirmar.
Ao terminar, rode npm run build e npx tsc --noEmit e me mostre o resultado.
```
